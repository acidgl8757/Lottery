#include "fileUtils.h"
#include "lotteryData.h"

FileUtils::FileUtils(LotteryData* data)
        : m_data(NULL)
{
}

bool FileUtils::loadFile(std::string path)
{
    return true;
}

bool FileUtils::saveFile(std::string path)
{
    return true;
}
